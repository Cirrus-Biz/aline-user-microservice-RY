pipeline {
    agent any

    stages {
        stage('Github connect') {
            steps {
                git 'https://github.com/Cirrus-Biz/aline-user-microservice-RY.git'
                sh 'ls'
            }
        }
    }
}